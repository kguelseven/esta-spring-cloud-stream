#!groovy
// Die unten genannten Einstellung sind so global in Jenkins definiert. Somit ist die untenstehende Methode auskommentiert.
// Falls das Versionshandling der helper und der pipeline selbst gemanagt werden soll kann dies hier gemacht werden. Jedoch auf eigenes Risiko.
//@Library(['pipeline-helper@release', 'esta-cloud-pipeline@release']) _

estaCloudPipeline([:])